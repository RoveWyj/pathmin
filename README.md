# pathmin

> 用 MoonBit 编写的 **SVG `<path>` 无损压缩器（minifier）** —— 压缩体积，形状不变。

为 Web 图标库、地图矢量、SVG 素材精简 `<path d="...">` 字符串，减少传输与存储体积，**完全不改变渲染出来的图形**。

## 特性

- **无损压缩**：只删除冗余信息，不损失任何精度
  - 去掉多余空格与逗号分隔符：`M 10, 20 L 30, 40` → `M 10 20 L 30 40`
  - 去掉小数尾零与末尾小数点：`10.0000` → `10`、`5.50` → `5.5`
  - 去掉正号：`+1.5` → `1.5`
  - 统一输出紧凑分隔
- **MoonBit 语言优势**：用 ADT（`enum Token`）+ 模式匹配实现词法分析，结构清晰、便于扩展
- **可测试**：核心逻辑纯函数，完整单元测试覆盖

## 效果对比

输入（常见人工格式化写法）：

```
M 24.000, 12.000 C 24.000, 5.373 18.627, 0.000 12.000, 0.000 Z
```

输出：

```
M 24 12 C 24 5.373 18.627 0 12 0 Z
```

| | 长度 |
|---|---|
| 原始 | 196 字节 |
| 优化后 | 111 字节 |
| 节省 | **43%** |

两个字符串渲染出的图形完全一致。

## 环境要求

- [MoonBit CLI](https://www.moonbitlang.cn/download/)（建议最新版）
- VSCode + MoonBit 插件（可选，提供语法高亮与补全）

验证安装：

```bash
moon version
```

## 快速开始

```bash
# 静态检查
moon check

# 运行单元测试
moon test

# 运行 CLI：传入一段 path d
moon run cmd/main "M 100.000,200.000 L 300.0000,400.0000 Z"

# 不带参数时使用内置示例
moon run cmd/main
```

示例输出：

```
input    : M 100.000, 200.000 L 300.0000, 400.0000 L 0.0000, 0.0000 Z
minified : M 100 200 L 300 400 L 0 0 Z
bytes    : 58 -> 27  (saved 53%)
```

> 说明：压缩器对「含冗余」（多余空格/逗号/尾零）的输入收益最大；本身已是紧凑写法的输入可能因规范化补空格而略有增大，属正常现象。

## 库用法

项目同时提供库 API，可被其他 MoonBit 包引用：

```moonbit
let r = @pathmin.optimize("M 10.000, 20.000 L 30.0000, 40.0000")
r.minified      // "M 10 20 L 30 40"
r.original_len  // 原始长度
r.new_len       // 优化后长度
```

## 项目结构

```
pathmin/
├── moon.mod            # 模块配置
├── moon.pkg            # 根库包
├── lib.mbt             # 核心：词法分析 + 数字精简 + 序列化 + 命令统计
├── lib_test.mbt        # 单元测试
├── cmd/
│   └── main/           # 可执行入口（CLI）
│       ├── main.mbt
│       └── moon.pkg
└── README.md
```

## Roadmap

- [ ] **隐含命令省略**：`M` 后的连续坐标省略 `L` 字母（`M 10 10 20 20`）
- [ ] **数字精度裁剪**：可配置保留 N 位小数（有损模式）
- [ ] **冗余段消除**：零长度段、重复闭合点的删除（可配置）
- [ ] **压缩率统计报告**：命令维度分析（当前已有 `command_counts`）
- [ ] **WASM 在线演示**：编译到 WebAssembly，浏览器内实时输入输出

## 许可证

[MIT](./LICENSE)