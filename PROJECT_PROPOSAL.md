# pathmin — 项目申报书

## 基本信息

- 项目名称：pathmin
- 参赛者：（待填写）
- 联系方式：（待填写）
- 项目方向：应用与内容工具 / SVG 矢量内容处理
- 是否为移植项目：否（原创项目）

## 项目简介

pathmin 是基于 MoonBit 实现的 SVG `<path>` 无损压器：通过解析 path 中的命令与坐标，去除尾零、多余分隔符与冗余数字，生成体积更小、渲染结果完全一致的 `d` 字符串。项目面向 Web 前端、图标库作者、地图与可视化开发者等需要在工程中精简 SVG 体积的使用者，提供命令行工具与可复用的 MoonBit 库，为后续扩展（隐含命令省略、精度裁剪等）保留结构化的解析管线。

## 核心功能范围

- 提供 `<path d>` 词法解析能力，完整识别 M/L/H/V/C/S/Q/T/A/Z 等命令与坐标数字；
- 支持数字无损精简：去除小数尾零、末尾小数点与正号（如 `10.0000` → `10`）；
- 支持紧凑序列化：统一去除多余空格与逗号分隔符，输出最小化字符串；
- 提供命令出现次数统计与压缩前后字节数对比，便于验证压缩收益；
- 提供统一入口 `optimize(d)`，返回精简结果与体积统计；
- 提供 CLI 入口，支持传入任意 path 字符串或使用内置示例；
- 提供针对解析、精简、序列化逻辑的单元测试，保证无损性不回归。

## 预期使用场景

1. **Web 图标库**：iconfont 类项目包含大量 path 数据，接入本工具可在发布期统一压缩，减少字体与符号文件体积，加快页面首屏加载。
2. **地图与矢量瓦片**：地图渲染依赖大量折线与多边形 path，压缩后可显著降低网络传输量，在弱网与移动端场景下作用明显。
3. **SVG 资源打包**：前端工程以字符串或文件形式引入 SVG，构建阶段对资源做无损压缩，可直接降低产物体积。
4. **单文件批量处理**：作为命令行工具批量精简多个 SVG 的 `d` 属性并输出压缩率，便于检查与对比。

## 原创性说明

本项目为原创项目，代码全部自研。压缩思路参考了开源社区常见的 SVG 优化手段（如 svgo 的去除尾零与分隔符），仅作思路参考，未直接移植源码。

## GitHub 仓库链接

待创建仓库后补充：<https://github.com/你的用户名/pathmin>

提交记录按功能开发分阶段提交，每条 commit 对应一次实质性变更（初始工程、词法解析、数字精简、序列化、测试、CLI、README、LICENSE、申报书等），累计不少于 10 个有效 commit。